<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto px-4 py-5">
        <div class="bg-white shadow rounded-lg p-6">
            <div class="grid md:grid-cols-2 gap-4">
                <div>
                    <img class="w-full object-cover h-64 rounded" src="<%= product.image %>" alt="<%= product.name %>">
                </div>
                <div>
                    <h2 class="text-xl font-bold mb-2"><%= product.name %></h2>
                    <p class="text-gray-700 mb-2"><%= product.description %></p>
                    <p class="text-indigo-500 text-xl font-bold"><%= product.price %></p>
                    <button class="mt-4 bg-indigo-500 text-white rounded py-2 px-4">Add to Cart</button>
                </div>
            </div>
        </div>
    </div>
</body>
</html>